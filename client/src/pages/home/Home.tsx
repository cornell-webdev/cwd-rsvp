import React from 'react'
import { useEvents } from 'src/api/event'
import styled from 'styled-components'

const Home = () => {
  // const { events } = useEvents()

  // const taggedEvents = events?.filter((event) => event.tag)
  // console.log('taggedEvents', taggedEvents)

  // const sampleEvents = events?.slice(0, 20)
  // console.log('sampleEvents', sampleEvents)

  return <Container>Home</Container>
}

const Container = styled.div``

export default Home
